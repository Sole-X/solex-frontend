export declare const WalletErrors: {
  NOT_INSTALLED: string;
  INTERNAL_ERROR: string;
  POLLING_TIMEOUT: string;
  INVALID_BODY: string;
  INVALID_PREPARE_TYPE: string;
  UNSUPPORTED_METHOD: string;
  EMPTY_ADDRESS: string;
};
